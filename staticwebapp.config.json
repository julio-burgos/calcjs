{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://xmelanoma.azurewebsites.net/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/**/*.{png,jpg,gif,css,js}", "/api/*"]
  }
}
